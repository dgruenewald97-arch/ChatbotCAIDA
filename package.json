{
  "name": "chatbot-caida",
  "version": "1.0.0",
  "private": true,
  "description": "CAIDA Mitsubishi conversational commerce concept prototype",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check lib/caida-ai.js && node --check lib/caida-http.js && node --check api/ai-status.js && node --check api/ai-chat.js && node --check api/demo-lead.js && node --check api/health.js && node scripts/smoke-api.js"
  },
  "engines": {
    "node": "22.x"
  }
}
